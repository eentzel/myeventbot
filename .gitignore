.DS_Store
*.pyc
# beacuse CSS files are all generated from .less files:
*.css
# because it contains private credentials:
test_token.txt
# because shell scripts are for local use, not for App Engine
*.sh
# because it's auto-generated:
version\.txt